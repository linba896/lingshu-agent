#!/usr/bin/env bash
# ============================================================
# 灵枢（LingShu）Agent — macOS 双击启动器
# 双击 .command 文件时，macOS 自动打开 Terminal 执行此脚本
# ============================================================

cd "$(dirname "$0")"
./start.sh
