# ============================================================
# 灵枢（LingShu）Agent — Python 依赖清单
# 整合：Phase 1-6 + 增补卷 + 进化卷
# ============================================================

# --- 基础运行时 ---
pyyaml>=6.0
click>=8.1.0
rich>=13.0.0
loguru>=0.7.0
gradio>=4.0.0          # 图形界面（灵枢台）
httpx>=0.24.0          # 异步HTTP客户端（硬件设备API等）

# --- 语音模块 (Phase 2 + 增补卷声纹) ---
faster-whisper>=1.0.0
webrtcvad>=2.0.10
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
transformers>=4.40.0
torch>=2.2.0
librosa>=0.10.0        # 声纹特征提取（MFCC等）
scikit-learn>=1.3.0    # 声纹相似度计算
# paddlespeech>=0.8.0  # 可选：韵律模型（较大，按需安装）

# --- 视觉模块 (Phase 4) ---
mss>=9.0.1             # 高性能跨平台屏幕捕获
Pillow>=10.0.0         # 图像处理（PIL/Image）
pytesseract>=0.3.10    # OCR 文本识别（降级分析，需安装 Tesseract 引擎）
# qwen-vl-utils>=0.0.2   # Qwen-VL 工具函数（VLM 推理辅助）

# --- 执行模块 (Phase 4) ---
pyautogui>=0.9.54
# pyauto-desktop>=0.1.0
# askui>=0.20.0

# --- 记忆模块 (Phase 5) ---
chromadb>=0.5.0
# sqlite-vec>=0.1.0
# sentence-transformers>=2.5.0

# --- 硬件控制 (增补卷十四 + 进化卷六) ---
pyserial>=3.5          # RS-232/RS-485串口通信
pymodbus>=3.5.0        # Modbus/RTU和Modbus/TCP
paho-mqtt>=1.6.0       # MQTT客户端协议
# pyusb>=1.2.0          # USB HID设备控制（需系统安装libusb）

# --- 系统监控 (观星术) ---
psutil>=5.9.0

# --- 开发工具 ---
pytest>=8.0.0
black>=24.0.0

# ============================================================
# 离线安装说明：
# 1. 在有网络的机器上：pip download -r requirements.txt -d wheels/
# 2. 将 wheels/ 目录复制到U盘
# 3. 在目标机器上：pip install --no-index --find-links=wheels/ -r requirements.txt
#
# 注意大型依赖：
#   torch: pip download torch --index-url https://download.pytorch.org/whl/cpu -d wheels/
#   transformers: 依赖 torch，需一起下载
# ============================================================
