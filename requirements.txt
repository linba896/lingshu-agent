# ============================================================
# 灵枢（LingShu）Agent — Python 依赖清单
# 版本：Phase 1-6 + 增补卷 + 进化卷
# ============================================================

# --- 基础运行时 ---
pyyaml>=6.0
click>=8.1.0
rich>=13.0.0
loguru>=0.7.0
gradio>=4.0.0              # GUI 面板（灵枢台）
httpx>=0.24.0             # HTTP 请求（硬件控制/网络通信）

# --- 语音交互 (Phase 2 + 增补卷声纹) ---
faster-whisper>=1.0.0
webrtcvad>=2.0.10
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
transformers>=4.40.0
torch>=2.2.0
librosa>=0.10.0          # 声纹特征提取（MFCC）
scikit-learn>=1.3.0      # 声纹相似度计算
# paddlespeech>=0.8.0    # 声纹模型Ecapa-TDNN（可选，需较大依赖）

# --- 视觉理解 (Phase 3) ---
# qwen-vl-utils>=0.0.2
# Pillow>=10.0.0
# mss>=9.0.1

# --- 执行操作 (Phase 4) ---
pyautogui>=0.9.54
# pyautodesktop>=0.1.0
# askui>=0.20.0

# --- 记忆学习 (Phase 5) ---
chromadb>=0.5.0
# sqlite-vec>=0.1.0
# sentence-transformers>=2.5.0

# --- 硬件控制 (增补卷硬件控制 + 进化卷) ---
pyserial>=3.5          # RS-232/RS-485 串口通信
pymodbus>=3.5.0        # Modbus/RTU协议Modbus/TCP
paho-mqtt>=1.6.0        # MQTT 通信
# pyusb>=1.2.0          # USB HID 设备（可选，需libusb）

# --- 系统监控 (Phase 1 + 主动服务) ---
psutil>=5.9.0

# --- 测试工具 ---
pytest>=8.0.0
black>=24.0.0

# ============================================================
# 安装说明（中国用户）：
# 1. 使用国内镜像加速安装：pip download -r requirements.txt -d wheels/
# 2. 在 wheels/ 目录离线安装
# 3. 使用国内镜像源安装：pip install --no-index --find-links=wheels/ -r requirements.txt
#
# 特定依赖安装建议（较大）：
#    torch: pip download torch --index-url https://download.pytorch.org/whl/cpu -d wheels/
#    transformers: 依赖torch，需先安装torch
# ============================================================
